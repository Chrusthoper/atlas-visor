# Frontera de decisión

La ecuación `w^T x + b = 0` dibuja la frontera entre las dos clases.

- 2D → una **recta**
- 3D → un **plano**
- nD → un **hiperplano**

Los pesos `w` controlan la **orientación** (pendiente) y el bias `b` controla el **desplazamiento** paralelo.
